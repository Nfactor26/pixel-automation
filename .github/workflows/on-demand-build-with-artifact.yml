name: Build on demand with artifact

on:
  workflow_dispatch:
  push:    
    tags:        
      - v*

jobs:

  setup-for-build:
    uses: ./.github/workflows/setup-for-build.yaml

  build-for-windows:
    needs: setup-for-build
    uses: ./.github/workflows/build-for-windows.yaml

  build-for-linux:
    needs: setup-for-build
    uses: ./.github/workflows/build-for-linux.yaml
  

 
